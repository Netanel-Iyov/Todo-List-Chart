apiVersion: v1
kind: Service
metadata:
  name: {{ include "Todo-List-Chart.fullname" . }}
  labels:
    {{- include "Todo-List-Chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Todo-List-Chart.selectorLabels" . | nindent 4 }}
